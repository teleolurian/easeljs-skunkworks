// Generated by CoffeeScript 1.12.6
(function() {
  var BASECHARS, Color;

  BASECHARS = '0123456789ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz';

  module.exports = Color = (function() {
    Color.prototype.colors = [Color.red, Color.green, Color.blue];

    function Color(red, green, blue) {
      this.red = red;
      this.green = green;
      this.blue = blue;
    }

    Color.prototype.darken = function(percent) {
      return this.lighten(percent * -1);
    };

    Color.prototype.hex = function(num) {
      return this.radix(num, 16, 2);
    };

    Color.prototype.hexcolor = function() {
      return '#' + this.hex(this.red) + this.hex(this.green) + this.hex(this.blue);
    };

    Color.prototype.lighten = function(percent) {
      var c, i, len, ref, results;
      ref = ['red', 'green', 'blue'];
      results = [];
      for (i = 0, len = ref.length; i < len; i++) {
        c = ref[i];
        results.push(this[c] = Math.floor(this[c] + 255 * (percent / 100)) % 255);
      }
      return results;
    };

    Color.prototype.radix = function(num, radix, minlength) {
      var ref, result;
      if (minlength == null) {
        minlength = 0;
      }
      result = '';
      while (num > 0) {
        result = ((ref = BASECHARS[num % radix]) != null ? ref : '?') + result;
        num = Math.floor(num / radix);
      }
      while (result.length < minlength) {
        result = '0' + result;
      }
      return result;
    };

    return Color;

  })();

}).call(this);
